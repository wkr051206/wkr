## 串的有关术语

**子串
串中任意连续的字符组成的子序

**子串的位置
s="ababcabcac",t="abc",子串T在主串s中的位置为3

**串相等
两个串相等，当且仅当两个串长度相同，并且各个对应位置的字符都相同

## 串的基本操作

1. 串赋值操作assign(s,t)功能，将串t的值赋给串变量s
2. 串相等equal(s,t)函数
3. 串的联接操作concat(s,t)把t接在串s后面
4. 求串长操作length(s);
5. 求子串sub(s,start,len,t)