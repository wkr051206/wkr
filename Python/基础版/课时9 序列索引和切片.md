六个标准的数据类型中是序列的有：字符串（String)，列表(List)，元组(Tuple)
通过索引和切片的方式可以访问序列中的元素
## 序列索引
![](media/Pasted%20image%2020250718104111.png)
会发现字符串中每一个元素都有两个索引，一个是正向一个是反向
![](media/Pasted%20image%2020250718105518.png)
![](media/Pasted%20image%2020250718111515.png)

## 序列切片
- 格式：[起始索引：结束索引：步长]（注意：所有符号都是英文输入法下的）
- 从序列中截取起始索引到结束索引的那部分字序列，包括起始索引，但不包括结束索引
- 当起始索引没有指定时，默认为0，当结束索引没有指定时，默认为序列的长度（前提：步长为正）
- 步长不写默认为1，代表切片时索引依次+1来选择元素，如果步长为2，则切片时索引依次+2来选择元素，如果步长为负数，则从还没开始切片，索引依次做减法
![](media/Pasted%20image%2020250718215221.png)
索引会降维，切片不会降维
![](media/Pasted%20image%2020250719155252.png)
每次索引都会降低一个维度
![](media/Pasted%20image%2020250719161046.png)

## len(s)
- 返回对象的长度（元素个数）
- s可以是序列（如string,bytes,tuple,list,range等）或集合（如dictionary,set或frozen set等）
![](media/Pasted%20image%2020250719161539.png)