## 行结构
- 一个Python程序可分为许多逻辑行，一般来说一个语句就是一行代码，不会跨越多行。
- 比如下面三行，每一行都通过print()输出一个结果
```
print(123)
print(456)
print(123456)
```

  否则程序会报错。
  如果一定要写一行可以中间加一个分号。
  
- 也有语句是可以跨越多行的，比如if else
```
 a=4
  if a<3:
  print(True)
  else:
  print(False)
```

- 有时复合语句也可以写在一行之内。
```
a=4
print(True) if a<3 else print(False)
```
## 缩进
缩进为4个格子（shift+Tab键）。
![](media/Pasted%20image%2020250713164026.png)
![](media/Pasted%20image%2020250714135156.png)
![](media/Pasted%20image%2020250714135250.png)

## 注释
注释在Python语法中会被忽略
- 单行注释：多以#开头
- 多行注释：每一行加一个#或者用’‘’或者“”“把注释内容包起来
选中的所有行加注释的快捷方式：ctrl+?键
![](media/Pasted%20image%2020250714182354.png)
## 拼接
- Python一般是一行写完一条语句，但如果语句很长，可以使用反斜杠(\)来实现多行语句。
![](media/Pasted%20image%2020250714182712.png)
- 隐式的行拼接：圆括号、方括号或花括号以内的多行语句，无需使用反斜杠(\)。
![](media/Pasted%20image%2020250714182832.png)

## 变量
- 变量三要素
变量名：指向值所在的内存你地址，通过他访问值
赋值符号（=）：赋值符号，用来将变量的内存地址绑定给变量名
值：变量的值就是内存地址对应存储的数据
- 标识符：用于变量，函数，类，模块等的名称
- 标识符命题规范：
1. 变量名只能包括字母，数字和下划线，且不能以数字开头。
2. 变量名不能包括空格，可以使用下划线来分隔多个单词
3. 不能使用Python的关键字及内置函数名作变量名
   关键字：
![](media/Pasted%20image%2020250714202935.png)
4. 变量名尽量短小精悍
5. 变量名区分大小写，比如Name和吧name是两个变量名
6. 驼峰命名法
![](media/Pasted%20image%2020250714202359.png)
定义一个常量：把变量名全部变成大写
单从语法层面上讲，常量与变量的使用完全一致。